# These dependencies execute required install hooks during a pnpm 11 source install.
# The consuming DSH profile must configure its own build policy separately.
allowBuilds:
  '@google/genai': true
  protobufjs: true
